# MEMORY — portfolio-aadi

## Goal
Build Aadi's personal portfolio website showcasing his shipped projects. Two phases:
- **Phase 1 (infra):** folder + boilerplate HTML → GitHub → Vercel auto-deploy → live URL. ← in progress
- **Phase 2 (design):** replace boilerplate with a premium `/ui-ux-pro-max` site matching the maths-agency aesthetic.

## Pipeline / resume state
- Folder: `~/Documents/portfolio-aadi/`
- GitHub: `aadityakalwani/portfolio-aadi` (auto-deploys on push to `main`)
- Vercel: project `portfolio-aadi` → `portfolio-aadi.vercel.app`
- **Content inventory** being generated by a background agent to: `<scratchpad>/PORTFOLIO_CONTENT_INVENTORY.md` — fold into Phase 2.
- Recovery rule: if the deploy is live and the repo exists, Phase 1 is done → jump to Phase 2. Re-run only the missing step.

## Decisions log
- **2026-07-16** — Stack = plain static HTML on Vercel, GitHub-linked auto-deploy. Rejected a framework (Next/Vite): a portfolio is static content, a build step is pure overhead here. Matches how his other sites (tutoring-with-aadi, maths-agency) already work, so the mental model is consistent.
- **2026-07-16** — Design North Star = `maths-agency.vercel.app`. Premium, dark, clean, non-AI-looking.
- **2026-07-16** — Phase 1 ships an intentionally minimal placeholder `index.html` first, to prove the CI/CD pipeline end-to-end before investing in design.

## Next session priorities
1. Confirm `portfolio-aadi.vercel.app` resolves.
2. Design interview (3-5 Qs) then `/ui-ux-pro-max` build using the content inventory.
